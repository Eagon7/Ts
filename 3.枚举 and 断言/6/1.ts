(() => {
  // let hd: null | string | undefined = 'houdunren';
  // hd = null;

  // const el: HTMLDivElement = document.querySelector('.hd') as HTMLDivElement;
  const el: HTMLDivElement = document.querySelector('.hd')!;
  console.log(el.innerHTML);


})()